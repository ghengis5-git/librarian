{
  "_comment": "Naming-enforcement hook ships DISABLED. The plugin is safe to install; no PreToolUse hook is registered. To enable naming enforcement: (1) copy hooks/hooks.enabled.example.json over this file, and (2) set project_config.enforce_naming_hook: true in the target project's REGISTRY.yaml. Both gates must be true for the hook to fire.",
  "hooks": {}
}
